# Installation

Copy repository.
```bash

```

Copy `config.default.toml` and `.env.default`.
```bash
cp config.default.toml config.toml
cp .env.default .env
```

Run docker-compose
```bash
docker-compose up
```
