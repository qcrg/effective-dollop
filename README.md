# Installation

Copy repository.
```bash
git clone https://github.com/qcrg/effective-dollop.git
```

Copy `config.default.toml` as `config.toml` and `.env.default` as `.env`.
```bash
cp config.default.toml config.toml
cp .env.default .env
```

Run docker-compose
```bash
docker-compose up
```
